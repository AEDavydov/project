name: Full Map Audit (manual only)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Manual placeholder; temporary audit workflow will be removed after results are collected.'
